{
  "name": "json-placeholder-test-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "npx mocha tests --timeout=10000",
    "test:html": "npx mocha tests --timeout=10000 --reporter spec --reporter mochawesome --reporter-options reportDir=results"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "mocha": "^10.4.0",
    "mochawesome": "^7.1.3",
    "pactum": "^3.6.7"
  }
}
