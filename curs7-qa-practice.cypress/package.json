{
  "name": "curs7-qa-practice.cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:headed": "npx cypress run --browser firefox --headed",
    "cy:headless": "npx cypress run --browser firefox",
    "api": "npx mocha api_tests/tests --timeout=10000",
    "api:html": "npx mocha api_tests/tests --timeout=10000 --reporter mochawesome --reporter-options reportDir=results,reportFilename=report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@testing-library/cypress": "^10.0.1",
    "cypress": "^13.8.1",
    "mocha": "^10.4.0",
    "mochawesome": "^7.1.3",
    "pactum": "^3.6.8"
  }
}
